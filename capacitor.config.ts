import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pafego.trivia',
  appName: 'pafego-trivia',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
